### CirclePackingCircleConfig

圆形配置

```typescript
export interface CirclePackingCircleConfig {
  /** 圆形间距 */
  padding?: number;
  /** 描边宽度 */
  strokeWidth?: number;
  /** 描边颜色 */
  strokeColor?: string;
  /** 填充透明度 0-1（用于实现半透明圆形效果，让底部背景图透出） */
  fillOpacity?: number;
}
```
